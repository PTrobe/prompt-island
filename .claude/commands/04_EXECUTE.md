# ⚡ /execute

Now implement precisely as planned, in full.

Implementation Requirements:
- Write elegant, minimal, modular code (Python for backend, React for frontend).
- Adhere strictly to existing code patterns (Pydantic models, strict JSON parsing, LLM retry loops).
- Include thorough, clear comments/documentation within the code.
- As you implement each step:
  - Update the markdown tracking document with emoji status and overall progress percentage dynamically.
